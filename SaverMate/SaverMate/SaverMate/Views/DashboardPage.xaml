<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="SaverMate.Views.DashboardPage">
  <Grid>
      <Grid.RowDefinitions>
          <RowDefinition Height="*"/>
          <RowDefinition Height="5*"/>
      </Grid.RowDefinitions>
      <StackLayout>
          <StackLayout>

              <StackLayout>
              <BoxView/>

          </StackLayout>

          <StackLayout>
              <BoxView/>
              <ProgressBar/>
          </StackLayout>

          <StackLayout>
              <BoxView/>
              <ProgressBar/>
          </StackLayout>

          </StackLayout>

          <StackLayout>
              <Label/>
              <Label/>
              <Label/>
          </StackLayout>
      </StackLayout>
  </Grid>
</ContentPage>